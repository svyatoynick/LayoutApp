import Foundation
import Models

extension Texts {
    
     public enum Frames {
        
        public static var title: String { NSLocalizedString("frames title", bundle: .module, comment: "") }
    }
}
