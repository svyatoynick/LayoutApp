import UIKit
import SparrowKit

enum Images {
     
    enum Menu {
        
        // В отдельной секции потому что
        // иконки будут использоваться много где.
        static var camera: UIImage { .init(.camera) }
        static var photo_library: UIImage { .init(.photo) }
    }
}
