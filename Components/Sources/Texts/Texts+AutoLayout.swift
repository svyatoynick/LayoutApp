import Foundation
import Models

extension Texts {
    
     public enum AutoLayout {
        
        public static var title: String { NSLocalizedString("auto layout title", bundle: .module, comment: "") }
    }
}
