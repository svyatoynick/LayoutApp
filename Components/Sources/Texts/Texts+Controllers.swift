import Foundation
import Models

extension Texts {
    
     public enum Controllers {
        
        public static var title: String { NSLocalizedString("controllers title", bundle: .module, comment: "") }
    }
}
